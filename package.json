{
  "scripts": {
    "build": "mkdir -p dist && npx postcss ./style.css -o ./dist/output.css",
    "vercel-build": "npm run build"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  },
  "devDependencies": {
    "@tailwindcss/postcss7-compat": "^2.2.17",
    "autoprefixer": "10.4.16",
    "postcss": "8.4.31",
    "tailwindcss": "3.3.3"
  }
}
