{
  "scripts": {
    "vercel-build": "npm run build",
    "build": "npm rebuild && postcss style.css -o dist/output.css",
    "postinstall": "chmod +x ./node_modules/.bin/postcss"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.4.31",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.1.4"
  }
}
